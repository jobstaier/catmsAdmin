{% extends 'CatMSAdminBundle::base.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li><a href="{{ path('content-group') }}">Content groups</a></li> <span class="divider">/</span>
    <li class="active">Create new group</a>
{% endblock %}        
        
{% block javascripts_bottom %}
    <script type="text/javascript" src="{{ asset('js/tinymce/tinymce.min.js')}}"></script>
    <script type="text/javascript" src="{{ asset('js/catms.text-editor.js')}}"></script>
    <script src="{{ asset('js/catms.fields-labels.js') }}"></script>
{% endblock %}          
        
{% block content -%}
    {% include 'CatMSAdminBundle:Helpers:form-errors-div.html.twig' with {'form': form, 'errors': errors | default() } %}
    
    <legend><i class="icon-large icon-pencil"></i> Content group creation</legend>
    
    <form novalidate action="{{ path('content-group-create') }}" method="post" {{ form_enctype(form) }}>
        <div class="btn-group" style="margin-bottom: 20px;">
            <a href="{{ path('content-group') }}" class="btn btn-small"><i class="icon-tags"></i> Back to the list</a>
            <button type="submit" class="btn btn-primary show-loader btn-small" data-loading-text="Creating..."><i class="icon-hdd icon-small"></i> Create</button>
        </div> 
        

        <div class="row-fluid">
            <div class="span5 input-wrapper">
                {{ form_row(form.slug, {'attr': {'class': 'span12'}}) }}
                {{ form_row(form.description, {'attr': {'class': 'span12'}})}}
                {{ form_row(form.isRemovable, {'attr': {'class': 'span12 select-icons'}})}}
            </div>
            <div class="span4 related-image-select pull-right offset3 input-wrapper">
                {{ form_row(form.relatedImages) }}
            </div>                    
        </div>
        
        <legend class="legend-inside"><i class="icon-large icon-tags"></i> Content group fields and labels</legend>
        <div class="content-group-fields">         
            <div class="row-fluid">  
                <div class="span3 chevronized">
                    {{ form_row(form.contentFields.hasDescription) }}
                </div>
                <div class="span6 field-label">
                    {{ form_row(form.contentFields.fieldDescriptionLabel) }}
                </div>
            </div>
            <div class="row-fluid">  
                <div class="span3 chevronized">
                    {{ form_row(form.contentFields.hasTitle) }}
                </div>
                <div class="span6 field-label">
                    {{ form_row(form.contentFields.fieldTitleLabel) }}
                </div>
            </div>
            <div class="row-fluid">  
                <div class="span3 chevronized">
                    {{ form_row(form.contentFields.hasFullText) }}
                </div>
                <div class="span6 field-label">
                    {{ form_row(form.contentFields.fieldFullTextLabel) }}
                </div>
            </div>                   
            <div class="row-fluid">  
                <div class="span3 chevronized">
                    {{ form_row(form.contentFields.hasShortText) }}
                </div>
                <div class="span6 field-label">
                    {{ form_row(form.contentFields.fieldShortTextLabel) }}
                </div>
            </div>        
        </div>
        
        <legend class="legend-inside"><i class="icon-large icon-comment"></i> Manual</legend>
        <div class="row-fluid">
            <div class="span12 input-wrapper">
                {{ form_widget(form.manual, {'attr': {'class': 'tiny_full'}}) }}
            </div>
        </div>
            
        {{ form_rest(form) }}
        
        <div class="btn-group">
            <a href="{{ path('content-group') }}" class="btn btn-small"><i class="icon-tags"></i> Back to the list</a>
            <button type="submit" class="btn btn-primary show-loader btn-small" data-loading-text="Creating..."><i class="icon-hdd icon-small"></i> Create</button>
        </div> 
        
    </form>
{% endblock %}
