{% extends 'CatMSAdminBundle::base.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li><a href="{{ path('image-group') }}">{{ 'assetGroup.title' | trans }}</a></li> <span class="divider">/</span>
    <li class="active">{{ 'assetGroup.createNewGroup' | trans }}</a>
{% endblock %} 

{% block content -%}
    <legend><i class="icon-large icon-pencil"></i> {{ 'assetGroup.createNewGroup' | trans }}</legend>

    <form novalidate action="{{ path('image-group-create') }}" method="post" {{ form_enctype(form) }} class="form-float content-manager-form">
        <div class="row-fluid">
            <div class="span6">
                {{ form_row(form.slug) }}
                {{ form_row(form.description) }}
                {{ form_row(form.imageWidth) }}
                {{ form_row(form.imageHeight) }}
            </div>
            <div class="span6">
                <label for="catms_adminbundle_imagegrouptype_hasThumbnails">Has thumbnails? {{ form_widget(form.hasThumbnails)}}</label><br />
                {{ form_row(form.thumbnailWidth) }}
                {{ form_row(form.thumbnailHeight) }}
            </div>
        </div>
        {{ form_rest(form) }}

        <br />
        <div>
            <a href="{{ path('image-group') }}" class="btn btn-small"><i class="icon-list"></i> {{ 'global.backToTheList' | trans }}</a>
            <button type="submit" class="btn btn-primary btn-small show-loader" data-loading-text="{{ 'global.creating' | trans }}"><i class="icon-hdd"></i> {{ 'global.create' | trans }}</button>
        </div>    
    </form>
{% endblock %}
